services:
  manga:
    build: .
    container_name: manga-reader
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./library:/library
    environment:
      - OCR_INTERVAL=300

