services:
  manga:
    build: .
    container_name: manga-reader
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - manga-library:/library
    environment:
      - OCR_INTERVAL=300

volumes:
  manga-library:
